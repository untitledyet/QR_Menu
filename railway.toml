[build]
builder = "nixpacks"

[deploy]
startCommand = "flask db upgrade && gunicorn manage:app --config gunicorn.conf.py"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10
healthcheckPath = "/health"
healthcheckTimeout = 10
