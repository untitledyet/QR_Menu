[build]
builder = "nixpacks"

[deploy]
startCommand = "python seed.py && python migrate_to_library.py && gunicorn manage:app --bind 0.0.0.0:$PORT"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10
